logging:
  file.name: ./log/backend-test.log
  level:
    root: INFO
    at.ac.tuwien.sepm.groupphase: DEBUG

spring:
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
  jpa:
    hibernate:
      ddl-auto: create